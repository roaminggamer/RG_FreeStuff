--[[

luaVersion -- Lua version used by SDK

onSimulator -- Running on the simulator?

platformVersion -- Platform version 
                -- (independent of make).

olderVersion -- 'true' if iOS device older than version 4

--]]